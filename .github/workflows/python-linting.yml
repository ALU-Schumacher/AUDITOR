name: python-linting

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
      - name: Run ruff lint check
        uses: astral-sh/ruff-action@v3
        with:
          version-file: "plugins/apel/pyproject.toml"
          args: 'check --target-version py39 --config plugins/apel/pyproject.toml --extend-select I --ignore FA100,BLE001'
      - name: Run ruff format check
        uses: astral-sh/ruff-action@v3
        with:
          version-file: "plugins/apel/pyproject.toml"
          args: 'format --check'
  black:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v7
      - name: Run black
        uses: psf/black@stable
        with:
          options: "--check --verbose"
